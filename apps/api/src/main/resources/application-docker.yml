# Profile: docker (activated via SPRING_PROFILES_ACTIVE=docker in docker-compose)
spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.mail.MailSenderAutoConfiguration
